变量声明
1.var  变量提升
<script>
//var a; //提前
console。log(a) //undefined
var a = 10; // a = 10;

2.var a = 20; 变量是允许重复定义的

3.不支持块级作用域

if(){
    块
    var i = 1；//全局变量：在整个script书写期间都能生效的变量

}
for(){

}
{

}
function(){

}
</script>

let

数据类型：
标量类型
Number
String
Bool
复合，应用类型
Array
Object
function
特殊类型
undefined
null
NaN (非数字)


系统函数(api)
toStringg()转化为字符串
praseInt()非数字类型转化为整数
parseFloat()非数字类型转化为数字
typeof   判断变量的数据类型

Number() 构造函数  产生数字对象
String() 构造函数  生成字符串对象
Array()  构造函数  生成数组对象
Object() 构造函数  生成 对象

new Number(10)--> 10
万物皆对象

let char = "asd";字符串对象----> 继承构造函数属性


？？？
如何判断数组是一个真实数组
返回值：接收到程序所返回的结果

运算符：
算数类：
+ - * / %
+(连接)

赋值类：
=
+=  a+b  ===> a= a+b
-=
/=
*=
%=

比较类
> < >= <= ==
=== 全等(比较变量的值与数据类型是否相等)
!=
!== 不全等

逻辑类
&& 逻辑与(都满足)  exp1 && exp2
|| 逻辑或(一个满足)   
! 逻辑非

自增自减：
前++ -- 先运算再判断
后++ -- 先判断在运算

运算符优先权
() 将一段表达式作为一个独立整体
具备最高执行权重

表达式：
a=5 && b=6
5 && b = 6   表达式 true
1true      2true  ===true
赋真值操作   赋真值操作
true           true ==true 
b=6
a:truea

三目运算符，三元表达式

？：
if(){

}else{

}

流程控制

循环

时间日期对象:
new Date();

new 实例化对象

let date = new Date();实例化 date 构造函数 产生时间日期对象

时间戳

1970 1 1 00:00:00 -当前 [毫]秒
date.getTime()

string 函数(api)：操作字符串的方法
方法：带括号  传参
属性：不带括号
indexOf() 检索某个字符首次出现的位置：返回匹配到的结果/-1
lastIndexOf() 检索某个字符最后出现的位置，返回匹配到的结果/-1
substring(index，start，end)字符串截取：索引位一直截取到末尾，包含截取位
substr(index,length)

repalce()字符串替换
split()字符串分割
trim()去除空格
tolowerCase()转小写
toupperCase()大写
